# =============================================================================
#  Djossi.ci — scraper/requirements.txt
#  Outils de scrape d'offres d'emploi en Côte d'Ivoire
# =============================================================================
# HTTP
requests>=2.32.0,<3
requests-cache>=1.2.0,<2          # Cache HTTP local (évite de re-scraper à chaque run)

# HTML parsing
beautifulsoup4>=4.12.0,<5
lxml>=5.2.0,<6                     # Parser HTML rapide + robuste

# Divers
python-slugify>=8.0.0,<9           # Slugs (titres, entreprises) pour détection de doublons
python-dateutil>=2.9.0,<3          # Parse dates relatives ("il y a 2 jours")

# Playwright — OPTIONNEL (pour sites SPA en JS).
# Décommenter la ligne ci-dessous PUIS exécuter :
#     playwright install chromium
# playwright>=1.45.0,<2
